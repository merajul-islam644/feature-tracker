{
  "schemaName": "Secret",
  "collectionName": "blx_Secrets",
  "schemaType": 1,
  "description": "Credential (email + masked password display) the AI verification agent uses to log into a configured target. The real password never reaches the cloud during the frontend phase — only a masked display string is persisted (see comments on `passwordMasked`).",
  "fields": [
    { "name": "name", "type": "String", "requiredOn": 3, "description": "Display label for the credential (e.g. 'QA Account')." },
    { "name": "email", "type": "String", "requiredOn": 3, "description": "Login email — free-form; not validated server-side." },
    { "name": "passwordMasked", "type": "String", "requiredOn": 3, "description": "Display-only masked string (•'s). The real password never leaves the user's browser today; only the masked echo is persisted. See spec section 12.3." }
  ]
}
