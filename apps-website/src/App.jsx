// Marketing-site entry. Single-page experience per DESIGN.md §15 —
// routing is intentionally absent until a second page is needed.
import Home from "./pages/Home.jsx";

export default function App() {
  return <Home />;
}
