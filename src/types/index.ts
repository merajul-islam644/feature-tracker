// Intentionally empty. The canonical data schemas live in
// src/types/Shemastructure/ for reference but are not imported
// anywhere in the app — consumers define the shapes they need inline.
export {};
